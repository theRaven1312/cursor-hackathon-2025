# API URL - Use this when deploying or accessing from external devices
# Leave empty for automatic detection based on hostname
# Example: VITE_API_URL=http://192.168.1.100:3001/api
VITE_API_URL=

# Enable HTTPS for Vite dev server (required for camera/geolocation on mobile)
# Set to 'true' and run ./generate-cert.sh to create certificates
VITE_HTTPS=false
